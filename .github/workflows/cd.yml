name: cd

on:
  push:
    branches: [main]

jobs:
  deploy:
    name: Deploy
    runs-on: ubuntu-latest

    steps:
      - name: Check out code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v4
        with:
          go-version: "1.20"

      - name: Build Prod
        run: ./scripts/buildprod.sh

      - id: "auth"
        uses: "google-github-actions/auth@v2"
        with:
          credentials_json: "${{ secrets.GCP_CREDENTIALS }}"

      - name: setup-gcloud
        uses: "google-github-actions/setup-gcloud@v2"

      - name: "Gcloud build submit"
        run: "gcloud builds submit --tag us-central1-docker.pkg.dev/notely-423507/notely-ar-repo/zsolomon88/notely:latest"

      - name: "Deploy to Cloud Run"
        run: "gcloud run deploy notely --image us-central1-docker.pkg.dev/notely-423507/notely-ar-repo/zsolomon88/notely@sha256:1724d8bc62e154a24b23ffdb844f34ac19729b3420947b6dd3bb91fd047ef052 --region us-central1 --allow-unauthenticated --project notely-423507 --max-instances=4"
